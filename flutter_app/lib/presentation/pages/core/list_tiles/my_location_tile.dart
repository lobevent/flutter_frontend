import 'package:flutter/material.dart';

class MyLocationTile extends StatelessWidget {
  const MyLocationTile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
