import 'package:flutter/material.dart';

class ProfilePageName extends StatelessWidget {
  const ProfilePageName({required Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
